<h1><% provide(:title,t('view.customers.customer_new')) %></h1>

<div class="container">
  <div class="upper-box">
    <%= render 'form', customer: @customer %>
    <%= link_to t('view.customers.to_index'), customers_path, class:"btn btn-info" %>
  </div>
</div>
