<%= form_with(model: @customer, local: true, url: choose_new_or_edit_customer ) do |form| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
      <% customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label t('view.customers.customer_number') %>
    <%= form.number_field :customer_number %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.customer')%>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.profession') %>
    <%= form.text_field :profession %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.customer_in_charge') %>
    <%= form.text_field :customer_in_charge %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.department') %>
    <%= form.text_field :department %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.position') %>
    <%= form.text_field :position %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.phone_number') %>
    <%= form.text_field :phone_number %>
  </div>

  <div class="field">
    <%= form.label t('view.customers.employee_in_charge')  %>
    <%= form.select :user_id, User.all.map{|o| [o.name, o.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
